---

- hosts: all
  become: true
  tasks:

  - name: Install Updates
    apt:
     upgrade: dist
     update_cache: yes
    changed_when: false
    when: ansible_distribution == "Ubuntu"

 - name: Create a directory
   file:
     path: /home/argente/logs
     state: directory
     mode: 0755

  - name: Collecting Log Files
    fetch:
      src: /var/log/syslog
      dest: /home/argente/logs

  - name: Collecting Journalctl Files
    shell: journalctl > /home/argente/logs/journal.log
